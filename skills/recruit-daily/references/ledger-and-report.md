# 台账补录 + 日报

## 台账 `02-sourcing/dedup-ledger.csv`（唯一事实源）

精确去重依赖**来源记录键**，候选人的内部稳定身份依赖**候选人键**。补录前先查来源记录键；精确命中已有行 → **更新那行**（别新建重复行）。
姓名 + 应聘岗位只能提示疑似重复：同名、打码名和跨平台记录都可能碰撞，必须人工核对公司、职务、城市等证据后再合并。命中且状态已推进（如“已约谈”）时别降级、别重复计入，最多在备注记“今日重发/重触达”。

列（表头就是这几列，顺序固定）：

```
候选人键,序号,姓名,应聘岗位,现职/背景,评级,状态,来源轮次,来源记录键,去重置信度,简历路径,备注
```

- **候选人键**：首次建档时生成不可变的本地键（推荐 `cand_<UUID>`），不从姓名或联系方式推导。
- **序号**：接最后一行 +1（末号可能不连续，取更大值起）。
- **姓名**：保留平台显示名；它是展示字段，不承担唯一键职责。
- **应聘岗位**：用 CONTEXT「在招岗位」表里的岗位名，与既有行一致，别造同义词。
- **评级**：⭐~⭐⭐⭐（命脉强 = ⭐⭐⭐）。
- **状态**：`未初筛 / 待初筛 / 初筛通过 / 初筛不合适 / 观望 / 已打招呼 / 已回复 / 待确认 / 已约谈 / 面试中 / 已拒 / 已排除`。
  打了招呼 = `已打招呼`；仅记录待用户确认 = `待确认`；命中硬规则 = `已排除`（记原因，**不自动点平台"不合适"**）。
- **来源轮次**：`YYYYMMDD` + 渠道入口，如 `20260709boss推荐` / `20260709boss搜索` / `20260709liepin` / `2026070951job`。
- **来源记录键**：优先对 `平台 + 账号作用域 + 平台稳定记录 ID + jobRef` 计算 SHA-256；本地简历则使用文件 SHA-256。只保存 `sha256:<hex>`，不把平台原始 ID 当成跨渠道身份。
- **去重置信度**：`exact`（来源记录键一致）/ `probable`（多项辅助信息疑似同人，待人工确认）/ `unresolved`（证据不足，保持独立记录）。
- **简历路径**：下了简历才填（`runtime/` 下的相对路径）；没下留空。
- **备注**：技能摘要 + 关键判断；不要重复写平台原始记录 ID 或联系方式。

批量补录用脚本 append 时注意：Windows 控制台默认编码会崩中文/emoji，Python 先
`sys.stdout.reconfigure(encoding="utf-8")`；临时文件用绝对路径。补录后核验：无重复候选人键、来源记录键和序号，按岗位计数对得上。

## 日报（从台账同步，不另立事实源）

**载体二选一，结构相同：**
- 有 `lark-cli` 且已配置 → 飞书云文档：把内容写成文件落在 cwd 相对路径，
  `lark-cli docs +create --content @<相对路径> --as user`（`@file` 只认 cwd 相对路径；
  超长内容内联易触发限制，走文件）。建完把文档 URL 回给用户。
- 没有 → 本地 `runtime/reports/YYYY-MM-DD.md`，写完把路径回给用户。

**结构建议（非死模板）：**

1. 一句话总结（今天干了什么、最重要的 1-2 个人选）
2. 今日概况：未读处理数 / 寻源覆盖岗位与渠道 / 打招呼数 / 台账新增与更新数
3. 岗位优先级（从 CONTEXT 同步，提醒老板当前主攻方向）
4. **按岗人选表**：姓名 ｜ 年龄/学历 ｜ 背景技能 ｜ 评级 ｜ 来源（命脉信号加粗）
5. 渠道观察（今天哪边池子肥/额度情况/异常）
6. 待办与待确认（等用户拍板的排除名单、待授权的打招呼、CONTEXT 待定决策清单）

**对外红线**：CONTEXT 标注的商业敏感信息不进日报；平台打码名在日报里保持打码；
候选人联系方式不进日报正文（留在台账/简历文件里）。

## 关键词迭代闭环（每轮寻源后）

从本轮「初筛通过/强匹配」的真实简历反向提取有效搜索词，回填 `01-jd/_internal/<role>.md` 的关键词迭代表：
有效词 / 无效词 / 积极信号 / 拒绝模式。下一轮寻源**先读这张表**，让搜索越来越准。
